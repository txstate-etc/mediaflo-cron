version: '3.5'
services:
  mfcron:
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
      NODE_ENV: development
      DB_HOST: mssqlnodeqa1.qual.txstate.edu
      DB_PORT: 1451
      DB_USER: sa-vcmsvideoqa
      DB_DOMAIN: QUAL
      DB_PASS: set in docker-compose.override.yml
      DB_DATABASE: ITS-VCMS
    volumes:
      - ./src:/usr/app/src
